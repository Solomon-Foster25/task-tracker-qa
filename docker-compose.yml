services:
  db:
    image: postgres:16
    environment:
      - POSTGRES_USER=Admin
      - POSTGRES_PASSWORD=password123
      - POSTGRES_DB=taskdb
    ports:
      - "5432:5432" 
    volumes:
      - shared-volume:/var/lib/postgresql/data

volumes:
  shared-volume: 